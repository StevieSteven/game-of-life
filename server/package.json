{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir build && npx babel src --out-file build/server.js",
    "start": "node build/server.js",
    "startDevelopment": "./node_modules/.bin/babel-node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-start": "npm i && npm start"
  },
  "author": "Stephan Strehler",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-latest": "^6.24.1"
  }
}
